// This file exports the MCP functions provided by the system
export const mcp_tavily_mcp_tavily_search = async (params: {
  query: string;
  search_depth?: 'basic' | 'advanced';
  include_raw_content?: boolean;
  max_results?: number;
  time_range?: 'day' | 'week' | 'month' | 'year' | 'd' | 'w' | 'm' | 'y';
  topic?: 'general' | 'news';
}) => {
  // The actual implementation is provided by the system
  // This is just a type definition for TypeScript
  return {
    results: [],
  };
}; 